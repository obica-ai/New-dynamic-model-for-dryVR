# Required
version: 2

build:
  apt_packages:
    - apt-https-transport
    - ca-certificates  # Explicitly install to update ca-certificates
    - openssl

formats: []

# Build documentation in the docs/ directory with Sphinx
sphinx:
   configuration: docs/source/conf.py

python:
   install:
   - requirements: docs/requirements.txt
